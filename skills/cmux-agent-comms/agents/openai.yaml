interface:
  display_name: "cmux Agent Comms"
  short_description: "Route XML messages between cmux agents."
  default_prompt: "Use $cmux-agent-comms to identify surfaces, spawn or connect to peer agents, and exchange XML-framed messages."
